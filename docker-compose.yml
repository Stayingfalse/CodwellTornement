version: '3.8'

services:
  codenames-bot:
    build: .
    env_file:
      - .env
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-80}:${WEB_PORT:-80}"
    volumes:
      - ./data:/app/data